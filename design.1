


0. Go tool / contracts

	eth_gtc --abi <file> --abi <file> --cfg Cfg.json 

	>> list [Contract]
		-- shows functions that are callable (from abi)
	>> events [contract]
		Show events and data for contract (from abi)

	>> x <name> ( Params )

	>> -- do something with addresses of functions (where loaded)
	
	>> unlock 
		-- do an unlock on account so can call / migrate

	>> send Acct Funds

	>> rv 
		-- get return value from function .call

	Shows events that result with output and Tx from calls


